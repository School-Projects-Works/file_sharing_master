services:
  powersync:
    container_name: powersync_demo
    restart: unless-stopped
    image: journeyapps/powersync-service:latest
    volumes:
      - ./powersync.yaml:/app/powersync.yaml
    networks:
      - supabase_network_filenet
    env_file: ../.env.local
    ports:
      - 8080:8080
      - 9090:9090

networks:
  supabase_network_filenet:
    external: true
